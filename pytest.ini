[pytest]
asyncio_mode = auto
# 連線池是 session 範圍的，測試與 fixture 必須共用同一個事件迴圈
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
